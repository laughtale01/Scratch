# 🚀 クイックデプロイメントガイド

## 1. ローカルテスト環境へのデプロイ（5分）

### Windows
```batch
# 1. ビルドとパッケージ作成
deploy-minecraft.bat

# 2. Minecraftへインストール
copy release\minecraft-collaboration-v1.0.0\mods\*.jar %APPDATA%\.minecraft\mods\

# 3. Minecraft起動
# Minecraft Launcher → Forge 1.20.1を選択 → プレイ
```

### Mac/Linux
```bash
# 1. ビルドとパッケージ作成
chmod +x deploy.sh
./deploy.sh

# 2. Minecraftへインストール
cp release/minecraft-collaboration-v1.0.0/mods/*.jar ~/.minecraft/mods/

# 3. Minecraft起動
```

## 2. サーバーへのデプロイ（10分）

### 1. サーバー準備
```bash
# Minecraft Forgeサーバーのダウンロード
wget https://maven.minecraftforge.net/net/minecraftforge/forge/1.20.1-47.1.0/forge-1.20.1-47.1.0-installer.jar

# サーバーインストール
java -jar forge-1.20.1-47.1.0-installer.jar --installServer

# Modのコピー
cp minecraft-collaboration-mod-1.0.0.jar ./mods/
```

### 2. サーバー設定
`server.properties`:
```properties
# ポート設定
server-port=25565
# その他の設定...
```

`minecraft-collaboration.toml`:
```toml
[general]
websocket_port = 14711
allow_external_connections = true  # サーバーの場合はtrue
```

### 3. サーバー起動
```bash
# 起動スクリプト
java -Xms4G -Xmx8G -jar forge-1.20.1-47.1.0.jar nogui
```

## 3. Firebaseへのデプロイ（5分）

### 事前準備
- Firebaseアカウント
- プロジェクト作成済み

### デプロイ手順
```bash
# 1. Firebase CLIインストール（初回のみ）
npm install -g firebase-tools

# 2. ログイン
firebase login

# 3. プロジェクト初期化（初回のみ）
firebase init

# 4. デプロイ
firebase deploy --only hosting

# または既存のバッチファイルを使用
deploy.bat
```

## 4. GitHub Releasesへの公開（5分）

### 1. リリース作成
```bash
# タグ作成
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0
```

### 2. GitHubでリリース作成
1. リポジトリページ → Releases → Draft a new release
2. タグを選択: v1.0.0
3. リリースタイトル: Minecraft Collaboration Mod v1.0.0
4. ファイルをアップロード:
   - `minecraft-collaboration-v1.0.0.zip`
   - `minecraft-collaboration-v1.0.0.zip.sha256`

### 3. リリースノート例
```markdown
## 🎉 新機能
- WebSocket通信によるScratch連携
- 7言語対応の多言語サポート
- 教育モード機能
- コラボレーション機能

## 📦 インストール
1. Minecraft Forge 1.20.1をインストール
2. modsフォルダにJARファイルをコピー
3. Minecraftを起動

## ✅ テスト済み環境
- Minecraft 1.20.1
- Forge 47.1.0
- Java 17
```

## 5. 動作確認チェックリスト

### ✅ 基本動作
- [ ] Minecraftが正常に起動する
- [ ] Modがロードされる（Mod一覧で確認）
- [ ] WebSocketサーバーが起動する（ログで確認）

### ✅ 機能確認
- [ ] `/collabhelp`コマンドが動作する
- [ ] Scratch拡張機能が接続できる
- [ ] ブロック配置コマンドが動作する
- [ ] 言語切り替えが動作する

### ✅ パフォーマンス
- [ ] FPSの大幅な低下がない
- [ ] メモリ使用量が適切
- [ ] WebSocket通信の遅延が許容範囲

## 6. トラブルシューティング

### 問題: Modが読み込まれない
```bash
# ログ確認
cat .minecraft/logs/latest.log | grep minecraft-collaboration

# 解決策
- Forgeバージョンを確認
- JARファイルの破損を確認
- 他のModとの競合を確認
```

### 問題: WebSocketサーバーが起動しない
```bash
# ポート確認
netstat -an | grep 14711

# 解決策
- ファイアウォール設定を確認
- 他のアプリケーションがポートを使用していないか確認
- 設定ファイルでポート番号を変更
```

### 問題: Scratch拡張機能が接続できない
- ブラウザコンソールでエラーを確認
- `ws://localhost:14711`にアクセス可能か確認
- CORSポリシーを確認

## 📞 サポート

問題が解決しない場合：
1. GitHubのIssuesに報告
2. ログファイルを添付
3. 環境情報を記載

---
最終更新: 2025-07-30