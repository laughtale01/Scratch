# 🧪 Minecraft協調学習システム - テストガイド
## 作成日: 2025年7月28日

---

## 📋 概要

本ドキュメントは、プロジェクトのテスト実行、カバレッジ測定、品質チェックの手順をまとめたものです。

---

## 🎯 テスト構成

### テストの種類

1. **単体テスト (Unit Tests)**
   - 個々のクラス・メソッドのテスト
   - Mockito を使用した依存関係のモック
   - 高速実行

2. **統合テスト (Integration Tests)**
   - 複数コンポーネントの連携テスト
   - `@Tag("integration")` でマーク
   - 実際の動作フローの検証

3. **パフォーマンステスト**
   - BatchBlockPlacer の性能測定
   - 大規模データでの動作確認

---

## 🚀 テスト実行方法

### 1. すべてのテストを実行

```bash
cd minecraft-mod
./gradlew test
```

### 2. 単体テストのみ実行

```bash
./gradlew unitTest
```

### 3. 統合テストのみ実行

```bash
./gradlew integrationTest
```

### 4. 特定のテストクラスを実行

```bash
./gradlew test --tests "CollaborationManagerTest"
./gradlew test --tests "*WebSocket*"
```

### 5. テストレポートの確認

```bash
# HTMLレポートを開く（Windows）
start minecraft-mod\build\reports\tests\test\index.html

# HTMLレポートを開く（Mac/Linux）
open minecraft-mod/build/reports/tests/test/index.html
```

---

## 📊 カバレッジ測定

### 1. カバレッジレポート生成

```bash
cd minecraft-mod
./gradlew test jacocoTestReport
```

### 2. カバレッジレポートの確認

```bash
# HTMLレポートを開く（Windows）
start minecraft-mod\build\reports\jacoco\html\index.html

# HTMLレポートを開く（Mac/Linux）
open minecraft-mod/build/reports/jacoco/html/index.html
```

### 3. カバレッジ目標

- **全体**: 80% 以上
- **重要クラス**: 90% 以上
  - CollaborationManager
  - WebSocketHandler
  - CollaborationMessageProcessor
  - BatchBlockPlacer

---

## 🔍 コード品質チェック

### 1. すべての品質チェック実行

```bash
cd minecraft-mod
./gradlew qualityCheck
```

### 2. 個別ツール実行

#### Checkstyle（コーディング規約）
```bash
./gradlew checkstyleMain
# レポート: build/reports/checkstyle/main.html
```

#### PMD（コード品質）
```bash
./gradlew pmdMain
# レポート: build/reports/pmd/main.html
```

#### SpotBugs（バグ検出）
```bash
./gradlew spotbugsMain
# レポート: build/reports/spotbugs/main.html
```

---

## 📁 テストファイル構成

```
minecraft-mod/src/test/java/com/yourname/minecraftcollaboration/
├── collaboration/
│   └── CollaborationManagerTest.java      # 協調機能のテスト
├── network/
│   └── WebSocketHandlerTest.java          # WebSocket通信のテスト
├── performance/
│   └── BatchBlockPlacerTest.java          # バッチ処理のテスト
└── integration/
    └── CollaborationSystemIntegrationTest.java  # 統合テスト
```

---

## ✅ 作成済みテスト

### 1. CollaborationManagerTest
- ✓ 招待の作成・管理
- ✓ 訪問リクエストの処理
- ✓ ワールド管理
- ✓ ホーム位置の保存・復帰
- ✓ 緊急帰宅機能

### 2. WebSocketHandlerTest
- ✓ サーバー起動・停止
- ✓ 接続管理
- ✓ IPアドレス制限
- ✓ レート制限
- ✓ メッセージ処理

### 3. BatchBlockPlacerTest
- ✓ ブロックのキューイング
- ✓ バッチ処理
- ✓ エラーハンドリング
- ✓ パフォーマンス測定
- ✓ チャンク更新

### 4. CollaborationSystemIntegrationTest
- ✓ 完全な招待フロー
- ✓ 訪問リクエスト処理
- ✓ 並行処理
- ✓ エラーケース
- ✓ レガシー形式サポート

---

## 🐛 デバッグ方法

### 1. テストログの詳細表示

```bash
./gradlew test --info
./gradlew test --debug
```

### 2. 特定のテストのデバッグ

```bash
./gradlew test --tests "CollaborationManagerTest.testCreateInvitation" --debug-jvm
```

### 3. テスト失敗時の対処

1. スタックトレースを確認
2. `build/reports/tests/test/classes/` で詳細確認
3. モックの設定を確認
4. 依存関係の初期化順序を確認

---

## 🔧 トラブルシューティング

### テストが実行されない
```bash
# キャッシュクリア
./gradlew clean
./gradlew cleanTest
```

### OutOfMemoryError
```bash
# メモリ増加
./gradlew test -Dorg.gradle.jvmargs="-Xmx4G"
```

### クラスが見つからない
```bash
# 依存関係の再取得
./gradlew clean build --refresh-dependencies
```

---

## 📈 継続的改善

### 1. 新機能追加時
- 必ず対応するテストを作成
- カバレッジが低下しないことを確認
- 統合テストも検討

### 2. バグ修正時
- バグを再現するテストを先に作成
- 修正後、テストが通ることを確認
- リグレッションテストとして保持

### 3. リファクタリング時
- 既存のテストが通ることを確認
- パフォーマンステストで性能劣化がないか確認
- 新しい構造に合わせてテストも更新

---

## 🎯 次のステップ

1. **現在のカバレッジ測定**
   ```bash
   ./gradlew test jacocoTestReport
   ```

2. **不足しているテストの特定**
   - カバレッジレポートで赤い部分を確認
   - 重要なロジックを優先

3. **E2Eテストの追加**
   - 実際のMinecraft環境でのテスト
   - Scratch拡張との連携テスト

4. **パフォーマンステストの拡充**
   - 大規模建築のベンチマーク
   - メモリ使用量の測定

---

## 📝 まとめ

品質の高いソフトウェアを維持するため、以下を心がけましょう：

1. **テストファースト**: 機能実装前にテストを書く
2. **高カバレッジ**: 80%以上を維持
3. **定期的な実行**: コミット前に必ずテスト実行
4. **継続的改善**: テストも常に改善

```bash
# 推奨ワークフロー
./gradlew clean
./gradlew test
./gradlew jacocoTestReport
./gradlew qualityCheck
```

---

作成者: Claude Code
作成日: 2025年7月28日