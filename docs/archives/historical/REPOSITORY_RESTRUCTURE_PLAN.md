# 🏗️ リポジトリ構造整理計画

## 📋 概要
scratch-guiディレクトリに40,000以上のファイルが存在し、Gitパフォーマンスとメンテナンス性を著しく悪化させています。これを別リポジトリに分離します。

## 🎯 目標
1. **パフォーマンス改善**: Git操作の高速化
2. **メンテナンス性向上**: 論理的な構造による管理効率化
3. **開発効率向上**: 軽量化されたリポジトリでの快適な開発

## 📂 現在の構造分析

### scratch-guiの内容（40,000+ファイル）
- **package.json**: scratch-gui v5.1.88（公式Scratchリポジトリのフォーク）
- **ビルド済みファイル**: build/, dist/ディレクトリ
- **ソースコード**: src/ディレクトリ（コンポーネント、コンテナ、リデューサー）
- **依存関係**: node_modules/（未カウント、追加で数万ファイル）
- **テスト**: test/ディレクトリ
- **翻訳**: translations/ディレクトリ

### 主要な問題
1. **Git履歴の肥大化**: すべてのscratch-guiファイルが履歴に含まれる
2. **クローン時間**: 巨大なリポジトリサイズによる遅延
3. **開発効率**: ファイル検索、インデックス作成の遅延

## 🔄 分離戦略

### Phase 1: scratch-guiのサブモジュール化
```bash
# 1. scratch-guiを別リポジトリとして切り出し
cd D:\minecraft_collaboration_project
git subtree split --prefix=scratch-gui -b scratch-gui-branch

# 2. 新しいリポジトリの作成
mkdir ../scratch-gui-separate
cd ../scratch-gui-separate
git init
git pull ../minecraft_collaboration_project scratch-gui-branch

# 3. サブモジュールとして再追加
cd ../minecraft_collaboration_project
git rm -r scratch-gui
git commit -m "Remove scratch-gui for submodule conversion"
git submodule add https://github.com/[username]/scratch-gui-custom.git scratch-gui
```

### Phase 2: 最適化された構造
```
minecraft_collaboration_project/
├── minecraft-mod/          # Minecraft Modソース
├── scratch-extension/      # カスタムScratch拡張
├── docs/                   # プロジェクトドキュメント
├── scripts/                # ビルド・デプロイスクリプト
├── .gitmodules            # scratch-guiサブモジュール参照
└── README.md
```

## 🚀 実装手順

### Step 1: バックアップ作成
```bash
# 現在の状態を完全バックアップ
cd D:\
cp -r minecraft_collaboration_project minecraft_collaboration_project_backup
```

### Step 2: scratch-guiの抽出
```bash
# scratch-guiディレクトリを独立したGit履歴として抽出
cd D:\minecraft_collaboration_project
git subtree split --prefix=scratch-gui -b scratch-gui-history
```

### Step 3: 新リポジトリ作成
1. GitHubで `minecraft-scratch-gui` リポジトリを作成
2. 抽出した履歴をプッシュ

### Step 4: サブモジュール化
```bash
# scratch-guiをサブモジュールとして再追加
git submodule add https://github.com/[username]/minecraft-scratch-gui.git scratch-gui
git commit -m "Add scratch-gui as submodule"
```

### Step 5: ビルドスクリプトの更新
サブモジュール対応のビルドスクリプトに更新

## 📊 期待される効果

### Before
- リポジトリサイズ: ~500MB+
- `git clone`時間: 5-10分
- ファイル数: 40,000+

### After
- リポジトリサイズ: ~10MB
- `git clone`時間: 10-30秒
- ファイル数: ~200

## ⚠️ 注意事項

1. **ビルドプロセス**: サブモジュールの初期化が必要
   ```bash
   git clone --recursive [repository-url]
   # または
   git submodule update --init --recursive
   ```

2. **開発フロー**: scratch-guiの変更は別リポジトリでコミット

3. **CI/CD**: サブモジュールを考慮したパイプライン設定

## 🔧 代替案

### Option A: CDNからのscratch-gui利用
- scratch-guiをビルドしてCDNにホスト
- index.htmlから直接参照
- 利点: リポジトリから完全削除可能
- 欠点: カスタマイズが困難

### Option B: GitHub Packagesの利用
- scratch-guiをnpmパッケージとして公開
- package.jsonから参照
- 利点: 標準的なnpmワークフロー
- 欠点: プライベートパッケージレジストリの設定が必要

## 🎯 推奨アクション

1. **即時実行**: scratch-guiのサブモジュール化（Phase 1）
2. **中期**: CDNホスティングの検討（Option A）
3. **長期**: 必要最小限のscratch-guiカスタマイズ版の作成

この計画により、開発効率とリポジトリパフォーマンスが大幅に改善されます。