# 🧹 リポジトリクリーンアップ実行計画

## 📊 現状分析

### 発見した問題
1. **scratch-guiディレクトリ（414MB）がGit管理外**
   - 40,000+ファイル
   - .gitignoreで部分的に除外されているが、ディレクトリ自体は未追跡
   - 完全なscratch-guiのソースが含まれている

2. **不要なファイルの存在**
   - 多数の削除済みマークのファイル（git status）
   - 開発中に生成された一時ファイル

## 🎯 即時実行アクション

### Step 1: scratch-guiの完全除外
```bash
# .gitignoreに追加
echo "scratch-gui/" >> .gitignore
```

### Step 2: 削除済みファイルのクリーンアップ
```bash
# 削除マークされたファイルを確認してコミット
git add -u
git commit -m "Clean up deleted files"
```

### Step 3: 軽量版scratch-gui参照の実装

#### Option A: CDN利用（推奨）
`docs/index.html`を更新して、公式Scratch GUIをCDNから読み込む

#### Option B: 最小限のラッパー作成
```
scratch-gui-wrapper/
├── package.json (scratch-guiを依存関係として)
├── index.html
└── custom-extensions.js
```

## 🚀 実装手順

### 1. scratch-guiディレクトリの処理
```bash
# バックアップ（必要に応じて）
mv scratch-gui scratch-gui-backup

# または完全削除
rm -rf scratch-gui
```

### 2. CDNベースの実装
`docs/index.html`を以下のように更新:
```html
<!-- TurboWarpまたは公式Scratchのiframe埋め込み -->
<iframe src="https://turbowarp.org/editor?extension=https://[your-domain]/minecraft-extension.js">
</iframe>
```

### 3. 拡張機能の独立ホスティング
```
scratch-extension/
├── dist/
│   └── minecraft-extension.js (ビルド済み)
└── hosting/
    └── index.html (拡張機能のドキュメント)
```

## 📈 期待される効果

### Before
- リポジトリサイズ: 500MB+
- ファイル数: 40,000+
- クローン時間: 5-10分

### After
- リポジトリサイズ: <10MB
- ファイル数: <300
- クローン時間: <30秒

## ⚠️ 注意事項

1. **scratch-guiのカスタマイズ**
   - カスタマイズが必要な場合は別リポジトリで管理
   - 本体プロジェクトでは参照のみ

2. **開発環境**
   - ローカル開発時はscratch-guiを別途クローン
   - または公式Scratch/TurboWarpを直接利用

3. **デプロイ**
   - 拡張機能のみをホスティング
   - Scratch GUI本体は外部サービスを利用

## 🎯 アクションアイテム

1. **即時**: scratch-guiディレクトリを.gitignoreに追加
2. **即時**: 削除済みファイルをクリーンアップ
3. **短期**: CDNベースのindex.html実装
4. **中期**: 拡張機能の独立したホスティング環境構築

この方針により、リポジトリを大幅に軽量化し、開発効率を向上させます。