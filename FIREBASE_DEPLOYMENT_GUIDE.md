# Firebase Hosting デプロイガイド

## 🚨 重要な注意事項

現在、`laughtale-scratch-ca803`プロジェクトへのアクセス権限が必要です。
以下の手順で正しいプロジェクトにデプロイしてください。

## 📋 前提条件

1. **Firebase CLIのインストール**
```bash
npm install -g firebase-tools
```

2. **Firebase認証**
```bash
firebase login
```

3. **プロジェクトへのアクセス権限確認**
- Firebase Console: https://console.firebase.google.com/u/5/project/laughtale-scratch-ca803/hosting/sites/laughtale-scratch-ca803
- プロジェクトへのアクセス権限があることを確認

## 🚀 デプロイ手順

### ステップ1: プロジェクトの準備

プロジェクトは既にビルド済みで、`public/`フォルダに必要なファイルが配置されています。

```bash
# プロジェクトディレクトリに移動
cd D:\minecraft_collaboration_project

# ファイルの確認
ls public/
```

### ステップ2: Firebaseプロジェクトの設定

`.firebaserc`ファイルは既に設定済み：
```json
{
  "projects": {
    "default": "laughtale-scratch-ca803"
  }
}
```

### ステップ3: デプロイ実行

```bash
# プロジェクトを明示的に指定してデプロイ
firebase deploy --only hosting --project laughtale-scratch-ca803
```

もしエラーが発生した場合：

```bash
# 1. ログアウトして再ログイン
firebase logout
firebase login

# 2. プロジェクトリストを確認
firebase projects:list

# 3. プロジェクトを設定
firebase use laughtale-scratch-ca803

# 4. 再度デプロイ
firebase deploy --only hosting
```

### ステップ4: 権限エラーの解決

「Permission denied」エラーが出る場合：

1. **Firebase Consoleで確認**
   - https://console.firebase.google.com にアクセス
   - `laughtale-scratch-ca803`プロジェクトが表示されるか確認
   
2. **プロジェクトオーナーに依頼**
   - プロジェクトの「設定」→「ユーザーと権限」
   - あなたのメールアドレスを追加（Editor以上の権限）

3. **新しいプロジェクト作成（代替案）**
```bash
# 新しいプロジェクトを作成
firebase projects:create scratch-minecraft-collab

# 新プロジェクトを使用
firebase use scratch-minecraft-collab

# Firebase Hostingを初期化
firebase init hosting

# デプロイ
firebase deploy --only hosting
```

## 📁 デプロイされるファイル

現在`public/`フォルダには以下のファイルが含まれています：

- **メインアプリ**: `index.html`, `gui.js`
- **ブロックエディタ**: `blocks-only.html`, `blocksonly.js`
- **プレイヤーモード**: `player.html`, `player.js`
- **Minecraft拡張**: `static/extensions/minecraft-unified.js`
- **アセット**: `static/assets/` (画像、音声ファイル)
- **合計**: 1,709ファイル

## 🔍 デプロイ後の確認

### 1. URLアクセス
```
https://laughtale-scratch-ca803.web.app
https://laughtale-scratch-ca803.firebaseapp.com
```

### 2. 機能確認
- [ ] メインページが表示される
- [ ] 拡張機能ボタンが動作する
- [ ] Minecraft拡張が選択できる
- [ ] WebSocketブロックが表示される

### 3. コンソール確認
ブラウザの開発者ツール（F12）でエラーがないか確認

## 🛠️ トラブルシューティング

### 問題: "Failed to get Firebase project"
**解決方法**:
1. Firebase CLIを最新版に更新: `npm update -g firebase-tools`
2. キャッシュクリア: `firebase logout && firebase login`
3. VPNを使用している場合は無効化

### 問題: "Permission denied"
**解決方法**:
1. プロジェクトオーナーに権限付与を依頼
2. 正しいGoogleアカウントでログインしているか確認

### 問題: デプロイは成功したが、サイトが表示されない
**解決方法**:
1. 数分待つ（CDN反映に時間がかかる場合あり）
2. ブラウザキャッシュをクリア（Ctrl+F5）
3. Firebase Consoleでホスティング状態を確認

## 📝 代替デプロイ方法

### GitHub Pagesを使用
```bash
# gh-pagesパッケージをインストール
npm install --save-dev gh-pages

# package.jsonにデプロイスクリプト追加
"scripts": {
  "deploy": "gh-pages -d public"
}

# デプロイ実行
npm run deploy
```

### Netlifyを使用
1. https://app.netlify.com にアクセス
2. `public`フォルダをドラッグ&ドロップ
3. 自動的にURLが生成される

## 📞 サポート

問題が解決しない場合：
1. Firebase Support: https://firebase.google.com/support
2. プロジェクトオーナーに連絡
3. GitHub Issuesで報告

## ✅ チェックリスト

- [ ] Firebase CLIインストール済み
- [ ] Firebaseにログイン済み
- [ ] プロジェクトへのアクセス権限確認
- [ ] public/フォルダにビルドファイル存在
- [ ] .firebasercファイル設定済み
- [ ] firebase.jsonファイル設定済み

準備ができたら、上記の手順でデプロイを実行してください。